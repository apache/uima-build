Building from SVN Checkout:
---------------------------

Download and install Maven 3, and Java 1.5, if needed.

Go into the parent-pom-top directory, and issue the maven command:

   mvn clean install
   
A list of issues fixed is in issuesFixed/jira-report.html.
   
